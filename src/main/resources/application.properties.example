# ===================================================================
# Application Properties Example
#
# 将此文件复制到同一目录下，并重命名为 'application.properties'，然后填入你本地的配置信息。
# 'application.properties' 文件应被 .gitignore 忽略，不应提交到版本库。
# ===================================================================

# 服务器端口
server.port=8080

# Spring 应用名称
spring.application.name=second-hand-transaction-backend

# ===================================================================
# 数据库配置
#
# 请替换为您本地的数据库设置。
# ===================================================================
spring.datasource.url=jdbc:mysql://localhost:3306/second_hand_transaction?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=your_database_username
spring.datasource.password=your_database_password

# ===================================================================
# MYBATIS-PLUS 配置
# ===================================================================
mybatis-plus.mapper-locations=classpath*:mapper/*.xml
